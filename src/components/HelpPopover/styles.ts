import styled from "@emotion/styled";
import HelpIcona from "@mui/icons-material/Help";
import { Box } from "@mui/material";

export const Logo = styled(Box)`
  cursor: pointer;
`;

export const HelpIcon = styled(HelpIcona)`
  cursor: pointer;
`;
